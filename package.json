{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev:all": "concurrently \"cd backend && npm run start:dev\" \"cd frontend && npm run dev\"",
    "start": "npm run dev:all"
  }
}
